 class RegistrationInformation{
   String email;
   String lastName;
   String firstName;
   String password;

   // checking the register infos in the first register screen and transfer them from a screen to another
   RegistrationInformation.registerInfos(this.email, this.password, this.lastName, this.firstName);
 }